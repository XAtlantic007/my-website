# AI普拉提会员成长系统

这是一个本地优先的静态网页项目，可部署到 GitHub Pages。

## 使用方式

- 线上访问：通过 GitHub Pages 打开 `index.html`
- 本地访问：双击 `index.html`
- 本地代理：如需使用图片接口代理，可运行 `node server.js` 后访问 `http://127.0.0.1:4173/`

## 注意

GitHub Pages 只托管静态文件，`server.js` 不会在线上运行。画图功能在线上会直接请求配置的图片生成接口。
